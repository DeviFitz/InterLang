NAME #get user input and put it on the stack
TIP #get user input as integer
GREETING #puts characters ASCii's values on the stack
ORDER #print the stack as ASCii
STRIGHTORDER #print stack
RECEIPT #prints everything on the stack as an integer
POUR #reverse everything on the stand
MOCHA #be able to a*b two items on the stack
BLACK #one to end the program
